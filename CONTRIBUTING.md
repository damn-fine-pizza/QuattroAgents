# Contributing

Use a local `.venv` only. Run `.venv/bin/python -m pytest`, `ruff check .`, `ruff format --check .`, `mypy src`, and `build` before submitting changes. Do not bypass protected-kernel gates or force-push.
